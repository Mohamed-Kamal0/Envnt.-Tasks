/** Same shape as Day 2 — only where it comes from changes today. */
export interface Product {
  id: number;
  name: string;
  //title: string;
  price: number;
  inStock: Boolean;
  category:string;
  //sku: string;
  //description: string;
}
