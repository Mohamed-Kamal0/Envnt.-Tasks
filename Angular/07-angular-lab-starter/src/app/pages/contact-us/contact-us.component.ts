import { Component } from '@angular/core';

/**
 * #Task 7: Build a page section with a heading reading "Contact Us" and a
 * compact card-like form below it, containing an email input, a multi-line
 * message input, and a "Send" button that isn't wired up to any logic yet
 * (that comes later in the course).
 */
@Component({
  selector: 'app-contact-us',
  standalone: true,
  templateUrl: './contact-us.component.html',
  styleUrl: './contact-us.component.css',
})
export class ContactUsComponent {}
